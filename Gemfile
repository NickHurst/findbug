# frozen_string_literal: true

source "https://rubygems.org"

# Specify your gem's dependencies in findbug.gemspec
gemspec

gem "rake", "~> 13.0"

gem "rspec", "~> 3.0"

gem "rubocop", "~> 1.21"

# SQLite is used as an in-memory database when running the test suite.
gem "sqlite3", ">= 1.4"
